# Continuous Integration Pipeline
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: "3.12"

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run Ruff linter
        run: uv run ruff check . --output-format=github

      - name: Run Ruff formatter check
        run: uv run ruff format --check .

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run tests with coverage
        run: uv run pytest --cov=jama_graphrag_api --cov-report=xml --cov-report=html
        env:
          # Use test values for unit tests (mocked)
          NEO4J_URI: neo4j://localhost:7687
          NEO4J_USERNAME: neo4j
          NEO4J_PASSWORD: test
          OPENAI_API_KEY: sk-test
          # Disable LangSmith tracing for unit tests (mocked LLMs produce 0-token noise)
          LANGSMITH_TRACING: "false"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage.xml
          fail_ci_if_error: false

      - name: Upload coverage HTML report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: htmlcov/

  # NOTE: Docker build and prompt validation jobs are temporarily disabled
  # after project restructure for Vercel deployment. MCP server and evaluation
  # modules were moved to separate packages.
  #
  # build-docker:
  #   name: Build Docker Image
  #   ...
  #
  # prompt-validation:
  #   name: Tier 1 - Prompt Validation
  #   ...

  # NOTE: Integration tests and smoke evaluation jobs are temporarily disabled
  # after project restructure for Vercel deployment. These jobs depend on
  # evaluation modules that were moved to separate packages.
  #
  # integration-test:
  #   name: Integration Tests
  #   ...
  #
  # smoke-evaluation:
  #   name: Tier 2 - Smoke Evaluation
  #   ...
